<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "customer_konnect" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "slate", :media => "all" %>

    <%= yield(:page_stylesheet) if content_for?(:page_stylesheet) %>

  </head>
  <body>

    <!-- Fixed navbar -->
    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href=<%= home_path %>>Customer Konnect</a>       
        
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
           <!-- <li class="active"><a href=<%= home_path %>>Home</a></li>-->
            <li><a href=<%= about_path %>>About</a></li>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Business <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <% if @current_user == nil %>
                  <li><a href="<%= log_in_path  %>">Login</a></li>
                  <%end%>
                  <% if @current_user %>
                  <li><a href="<%= get_feedback_business_path(@current_user) %>">Feebacks</a></li>
                  <li><a href="<%= log_out_path %>">Logout</a></li>

                  <%end%>
                  <li><a href="<%= signup_path%>">Signup</a></li>
                </ul>
            </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Shoppers <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><a href="<%= give_feedback_path%>">Give Feeback</a></li>
                  <li><a href="<%= business_path%>">List of Businesses</a></li>
                </ul>
                <li><a href=<%= contact_path %>>Contact</a></li>
            
            </li>
          </ul>
        </div><!--/.nav-collapse -->
          
        
        
        </div><!--/.nav-collapse -->
      </div>
    </div>

    <div style="margin-top: 80px">
    <%= yield %>
    </div>
    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "slate" %>
    <%= yield(:page_javascript) if content_for?(:page_javascript) %>
  </body>
</html>
